@model IEnumerable<DoAn_555.chucvu>

@{
    ViewBag.Title = "List_ChucVu";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>List_ChucVu</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cv_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cv_ten)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cv_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cv_ten)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
